export default {
  nuxtClientInit({ dispatch }, data) {
    // const userId = data.route.query['user-id']
    // if (userId) {
    //   await dispatch('auth/isLogged', { userId }, { root: true })
    // }
  }
}
